version: 2

models:
  - name: tmp_category
    config:
      alias: category_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    description: "Temporary table for categories with duplicate flagging."
    columns:
      - name: category_id
        tests: [not_null]
      - name: category_name
        # tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_film
    config:
      alias: film_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    description: "Temporary table for films with duplicate flagging."
    columns:
      - name: film_id
        tests: [not_null]
      - name: title
        # tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_payment
    config:
      alias: payment_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: payment_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_customer
    config:
      alias: customer_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: customer_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_staff
    config:
      alias: staff_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: staff_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_address
    config:
      alias: address_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: address_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_rental
    config:
      alias: rental_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: rental_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_actor
    config:
      alias: actor_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: actor_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_inventory
    config:
      alias: inventory_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: inventory_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_film_category
    config:
      alias: film_category_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: film_id
        tests: [not_null]
      - name: category_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_film_actor
    config:
      alias: film_actor_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: film_id
        tests: [not_null]
      - name: actor_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_store
    config:
      alias: store_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: store_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_city
    config:
      alias: city_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: city_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_country
    config:
      alias: country_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: country_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]

  - name: tmp_language
    config:
      alias: language_tmp
      materialized: incremental
      incremental_strategy: append
      schema: bronze
    columns:
      - name: language_id
        tests: [not_null]
      - name: is_duplicated_file
        # tests: [not_null]
      - name: is_duplicated_version
        # tests: [not_null]"